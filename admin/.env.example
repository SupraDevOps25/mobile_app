# Base URL of the Supracarer API (NestJS). Include the /api/v1 prefix.
# Production (Railway) is the default in code; override here for local dev, e.g.
# NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
NEXT_PUBLIC_API_URL=https://supramobileapi-production.up.railway.app/api/v1
