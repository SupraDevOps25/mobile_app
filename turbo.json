{
  "$schema": "https://turbo.build/schema.json",
  "packages": ["api", "mobile"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
